# Feedback widget storage (Neon Postgres).
# Provision via the Vercel marketplace Neon integration — DATABASE_URL is
# auto-populated. Locally, paste a Neon connection string here.
# When unset, /api/feedback logs to stdout and returns ok:true so previews
# don't crash before the DB is wired.
DATABASE_URL=
